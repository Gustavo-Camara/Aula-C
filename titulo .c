#include <stdio.h>
    void man ()
    {
        int numero = 0;
        printf("Digite um numero: ");
        scanf("%d", &numero );
        if (numero >= 0)
        {
            printf("O numero e positivo");
        }
        switch case (numero < 0)
        {
            printf("O numero e negativo");
        }

    }